apiVersion: v1
kind: Service
metadata:
  name: yertool
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "yertool"
    port: 8080
  selector:
    app: yertool
