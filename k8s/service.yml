apiVersion: v1
kind: Service
metadata:
  name: doudise
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "doudise"
    port: 8080
  selector:
    app: doudise
