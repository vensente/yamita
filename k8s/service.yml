apiVersion: v1
kind: Service
metadata:
  name: dcokefej
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "dcokefej"
    port: 8080
  selector:
    app: dcokefej
