apiVersion: v1
kind: Service
metadata:
  name: aofanm
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "aofanm"
    port: 8080
  selector:
    app: aofanm
