apiVersion: v1
kind: Service
metadata:
  name: ceavent
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "ceavent"
    port: 8080
  selector:
    app: ceavent
