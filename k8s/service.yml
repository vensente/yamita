apiVersion: v1
kind: Service
metadata:
  name: coujikeo
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "coujikeo"
    port: 8080
  selector:
    app: coujikeo
