apiVersion: v1
kind: Service
metadata:
  name: bingafe
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "bingafe"
    port: 8080
  selector:
    app: bingafe
