apiVersion: v1
kind: Service
metadata:
  name: bunwa
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "bunwa"
    port: 8080
  selector:
    app: bunwa
