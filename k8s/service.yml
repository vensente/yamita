apiVersion: v1
kind: Service
metadata:
  name: bearan
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "bearan"
    port: 8080
  selector:
    app: bearan
