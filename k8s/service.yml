apiVersion: v1
kind: Service
metadata:
  name: vooldet
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "vooldet"
    port: 8080
  selector:
    app: vooldet
