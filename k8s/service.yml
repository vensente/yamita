apiVersion: v1
kind: Service
metadata:
  name: xoesyut
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "xoesyut"
    port: 8080
  selector:
    app: xoesyut
