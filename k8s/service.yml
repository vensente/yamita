apiVersion: v1
kind: Service
metadata:
  name: govie
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "govie"
    port: 8080
  selector:
    app: govie
