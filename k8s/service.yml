apiVersion: v1
kind: Service
metadata:
  name: example
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "example"
    port: 8080
  selector:
    app: example
