apiVersion: v1
kind: Service
metadata:
  name: flateng
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "flateng"
    port: 8080
  selector:
    app: flateng
