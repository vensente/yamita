apiVersion: v1
kind: Service
metadata:
  name: aliyea
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "aliyea"
    port: 8080
  selector:
    app: aliyea
