apiVersion: v1
kind: Service
metadata:
  name: something
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "something"
    port: 8080
  selector:
    app: something
