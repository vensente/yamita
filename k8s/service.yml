apiVersion: v1
kind: Service
metadata:
  name: cuipol
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "cuipol"
    port: 8080
  selector:
    app: cuipol
