apiVersion: v1
kind: Service
metadata:
  name: biugoig
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "biugoig"
    port: 8080
  selector:
    app: biugoig
