apiVersion: v1
kind: Service
metadata:
  name: fedoucu
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "fedoucu"
    port: 8080
  selector:
    app: fedoucu
