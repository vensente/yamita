apiVersion: v1
kind: Service
metadata:
  name: cause
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "cause"
    port: 8080
  selector:
    app: cause
