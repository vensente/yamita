apiVersion: v1
kind: Service
metadata:
  name: zerokell
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "zerokell"
    port: 8080
  selector:
    app: zerokell
