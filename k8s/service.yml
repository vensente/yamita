apiVersion: v1
kind: Service
metadata:
  name: dbupuch
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "dbupuch"
    port: 8080
  selector:
    app: dbupuch
