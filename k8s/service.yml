apiVersion: v1
kind: Service
metadata:
  name: miuzouya
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "miuzouya"
    port: 8080
  selector:
    app: miuzouya
