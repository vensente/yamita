apiVersion: v1
kind: Service
metadata:
  name: impulses
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "impulses"
    port: 8080
  selector:
    app: impulses
