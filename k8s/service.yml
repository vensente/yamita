apiVersion: v1
kind: Service
metadata:
  name: dcockerou
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "dcockerou"
    port: 8080
  selector:
    app: dcockerou
