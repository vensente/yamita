apiVersion: v1
kind: Service
metadata:
  name: cendoqa
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "cendoqa"
    port: 8080
  selector:
    app: cendoqa
