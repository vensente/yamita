apiVersion: v1
kind: Service
metadata:
  name: bighill
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "bighill"
    port: 8080
  selector:
    app: bighill
