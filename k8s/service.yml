apiVersion: v1
kind: Service
metadata:
  name: xoution
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "xoution"
    port: 8080
  selector:
    app: xoution
