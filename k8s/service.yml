apiVersion: v1
kind: Service
metadata:
  name: xitaoh
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "xitaoh"
    port: 8080
  selector:
    app: xitaoh
