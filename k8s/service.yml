apiVersion: v1
kind: Service
metadata:
  name: bierteo
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "bierteo"
    port: 8080
  selector:
    app: bierteo
