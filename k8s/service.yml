apiVersion: v1
kind: Service
metadata:
  name: yuerose
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "yuerose"
    port: 8080
  selector:
    app: yuerose
