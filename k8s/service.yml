apiVersion: v1
kind: Service
metadata:
  name: airfer
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "airfer"
    port: 8080
  selector:
    app: airfer
