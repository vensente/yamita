apiVersion: v1
kind: Service
metadata:
  name: husouji
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "husouji"
    port: 8080
  selector:
    app: husouji
