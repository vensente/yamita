apiVersion: v1
kind: Service
metadata:
  name: betasun
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "betasun"
    port: 8080
  selector:
    app: betasun
