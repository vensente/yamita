apiVersion: v1
kind: Service
metadata:
  name: maked
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "maked"
    port: 8080
  selector:
    app: maked
