apiVersion: v1
kind: Service
metadata:
  name: cuyading
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "cuyading"
    port: 8080
  selector:
    app: cuyading
