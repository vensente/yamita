apiVersion: v1
kind: Service
metadata:
  name: observe
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "observe"
    port: 8080
  selector:
    app: observe
