apiVersion: v1
kind: Service
metadata:
  name: danliya
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "danliya"
    port: 8080
  selector:
    app: danliya
