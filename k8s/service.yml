apiVersion: v1
kind: Service
metadata:
  name: teniyakieto
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "teniyakieto"
    port: 8080
  selector:
    app: teniyakieto
